server:
  port: 8083
spring:
  application:
    name: cms-service-flow
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/dtworkflow?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&nullCatalogMeansCurrent=true
    username: root
    password: Passw0rd!
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    sentinel:
      transport:
        dashboard: localhost:9092
        port: 8719
#flowable:
 # async-executor-activate: false
 # database-schema-update: true 自动生成flowable相关表 第一次生成后建议关闭提高运行速度
 # process-definition-location-prefix: classpath*:/flow/
    # 保存历史数据级别设置为full最高级别，便于历史数据的追溯
    #history-level: full
mybatis-plus:
  typeAliasesPackage: com.cms.workflow.entity
  mapper-locations: classpath*:com/cms/workflow/mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
management:
  endpoints:
    web:
      exposure:
        include: '*'